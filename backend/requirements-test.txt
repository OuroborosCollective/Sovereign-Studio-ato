# Single source of truth for every Sovereign backend test environment.
# Runtime dependencies stay aligned with the production backend image.
-r ../scripts/sovereign-backend/requirements.txt

# Test runner contract.
pytest>=9.1.1,<10
# Hang guard: any blocking test (regex ReDoS, socket waits) must fail loudly
# instead of burning CI minutes until the job-level timeout kills the runner.
pytest-timeout>=2.4.0,<3
