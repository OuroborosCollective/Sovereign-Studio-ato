{
  "target": "src/features/release/PlayReleaseChat.tsx",
  "source_ref": "sovereign/chatgpt/frontend-draft-pr-end-to-end-fix",
  "branch_base_ref": "sovereign/chatgpt/frontend-draft-pr-end-to-end-fix",
  "branch": "sovereign/chatgpt/frontend-draft-pr-end-to-end-fix",
  "expectedSha": "0cb0de9eaebb9930a7d7ac165b659a7933ff8b14",
  "commit_message": "fix(frontend): bind Draft PR resume to stable user identity",
  "pr_title": "fix(frontend): make Draft PR flow survive real repository runs",
  "pr_body": "Keeps one current-session repository job bound across long-running readback and reloads, resumes the same job instead of starting duplicates, preserves explicit Draft-PR publication consent, and removes the online intent interpreter as a hard blocker for user-owned repository actions.",
  "blocks": [
    {
      "search": "    if (!user || restoredRepositoryActionRef.current) return;",
      "replace": "    if (!user?.id || restoredRepositoryActionRef.current) return;"
    },
    {
      "search": "  }, [agentClient, user]);",
      "replace": "  }, [agentClient, user?.id]);"
    }
  ]
}
