# Ghost Pilot Checklist

- Removed single-model dependency.
- Added model fallback order.
- Added deterministic fallback path.
- Added command quality gate.
- Uses `com.arestudio.nocode.aab` as package default.
- Runs Sovereign guard commands before emulator work.
- Builds and installs debug APK before test execution.
- Uploads logs and generated sequence artifacts.
