# Approval sync required

The approval button must not report completion until the repository received a branch, commit, or pull request. If no repository sync happened, the UI must keep the flow open and show a clear error.
