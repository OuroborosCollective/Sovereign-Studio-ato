# Retired tombstone. The normal package scripts intentionally refuse all
# Wrangler operations; if this file is deployed manually it returns HTTP 410
# before reading a request or contacting any provider.
name = "sovereign-llm-proxy-retired"
main = "src/index.ts"
compatibility_date = "2024-01-01"
