name: Apply frontend Draft PR live guest assertion fix

on:
  push:
    branches:
      - sovereign/chatgpt/frontend-draft-pr-end-to-end-fix
    paths:
      - scripts/patches/pending/frontend-draft-pr-live-guest-assertion.json
      - .github/workflows/apply-frontend-draft-pr-live-guest-assertion.yml

permissions:
  contents: write
  pull-requests: write

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Apply exact guest-session canary assertion fix
        env:
          GITHUB_TOKEN: ${{ github.token }}
          REPO_FULL_NAME: ${{ github.repository }}
          BASE_BRANCH: main
          PATCH_BRANCH: sovereign/chatgpt/frontend-draft-pr-end-to-end-fix
          DRY_RUN: 'false'
        run: python3 scripts/patches/search_replace_runner.py scripts/patches/pending/frontend-draft-pr-live-guest-assertion.json
