# ⚠️ QUARANTINED: Zero-Touch Simulation Workflow
#
# THIS WORKFLOW IS QUARANTINED - DO NOT ENABLE AUTOMATED SCHEDULING
#
# Original behavior was unsafe:
# - Proxy-based device simulation for misleading engagement signals
# - Automated app interaction with random durations
# - Crash log processing claiming real bugs
#
# This workflow is kept as documentation only.
# For questions, see Issue #475 in the repository.

name: "⚠️ QUARANTINED: Zero-Touch Simulation (DO NOT RUN)"

on:
  workflow_dispatch:
    inputs:
      confirm_quarantine:
        description: 'I understand this workflow is quarantined and should not run unattended'
        required: true
        type: boolean

jobs:
  blocked:
    name: ❌ BLOCKED - Workflow Quarantined
    runs-on: ubuntu-latest
    steps:
      - name: 🚫 This workflow is quarantined
        run: |
          echo "=============================================="
          echo "⚠️  THIS WORKFLOW IS QUARANTINED"
          echo "=============================================="
          echo ""
          echo "Original issues (Issue #475):"
          echo "- Proxy-based simulation creating misleading engagement signals"
          echo "- Random device interaction claiming organic usage"
          echo "- Automated bug creation from simulated crash logs"
          echo ""
          echo "Safe alternative: Real user engagement metrics from"
          echo "production analytics with human interpretation."
          exit 1
