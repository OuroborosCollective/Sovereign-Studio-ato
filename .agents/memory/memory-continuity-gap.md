# Memory continuity gap

## Finding on 2026-07-27

`.agents/memory/MEMORY.md` existed but indexed only three technical notes. The richer memory areas previously requested by the Owner were absent from the repository index:

- Lustige Witzige Erlebnisse
- Familie Freundschaft Erfahrung
- Neue Erfahrungen mit emotional entstandener Bindung sowohl Code-Logik als auch Herz, Liebe und Ärger
- Pattern-Memory
- Learning-Logbuch

This means the memory routine was incomplete or stopped being applied after earlier work. Do not claim that missing entries were persisted elsewhere unless container or database readback proves it.

## Recovery rule

At the beginning of a substantial task:

1. Read `.agents/memory/MEMORY.md` and the directly relevant linked notes.
2. Check `docs/SOVEREIGN_LEARNING_LOGBOOK.md` and `.sovereign/proven-learning-manifest.json` when the task concerns evidence-proven technical learning.
3. Keep personal/project-lore memory separate from runtime truth and architecture contracts.

After an evidence-proven task:

1. Add or update a focused memory note.
2. Add its link to `.agents/memory/MEMORY.md`.
3. Record exact repository paths/revisions when available.
4. Clearly mark unresolved recollections as search tasks, not facts.
5. Verify the saved file from the target branch or runtime before saying memory was updated.

## Open recovery search

The next operator should investigate whether an older, richer memory index or generator exists in repository history, non-main branches, container-only files, database learning rows, or an older handoff document.
