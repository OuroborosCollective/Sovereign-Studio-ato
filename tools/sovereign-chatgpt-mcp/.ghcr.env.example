# Optional host-only registry credentials for a private GHCR package.
# Copy to /opt/sovereign-chatgpt-tools/.ghcr.env with mode 0600.
# This file is consumed only by the root installer and is never mounted into the MCP container.

GHCR_USERNAME=
GHCR_TOKEN=
