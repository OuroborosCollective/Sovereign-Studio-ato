[project]
name = "sovereign-github-patch-mcp"
version = "1.0.0"
description = "ChatGPT MCP connector for GitHub read tools plus controlled full-file replace and SEARCH/REPLACE draft PRs."
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.13.0",
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "httpx>=0.27.0",
    "pydantic>=2.7.0"
]

[tool.uv]
package = false
