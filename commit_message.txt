🧪 [Add ErrorBoundary component tests]

🎯 **What:** The testing gap addressed
The `ErrorBoundary` component lacked tests, particularly around the `componentDidCatch` lifecycle method which dynamically imports a `storageService` to log errors. The test environment was not fully configured for DOM tests using `jsdom`.

📊 **Coverage:** What scenarios are now tested
- Happy path: Renders children when no error occurs.
- Default fallback: Renders the default generic fallback UI.
- Custom fallback: Renders the custom fallback prop when provided.
- Error Logging: The asynchronous behavior within `componentDidCatch` is successfully covered, intercepting the dynamic module loading, preventing infinite async/timer loops by carefully patching `global.Date`, and validating that log appending to `ss_error_log` executes correctly.

✨ **Result:** The improvement in test coverage
The `ErrorBoundary` component is now fully tested, preventing regressions with confidence. The application also now has `@testing-library/react` and `vitest` properly configured with `jsdom` setup so other components can easily be tested.
